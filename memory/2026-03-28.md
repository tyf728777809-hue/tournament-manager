## Durable notes
- 用户当前主要项目：炉石赛事自动化系统。以飞书源头文档《炉石赛事自动化系统设计稿 v1》为准，战队赛 P0 主链路已闭环；下一阶段主任务应转向个人赛链路（P1）。
- 本地文档已同步：`炉石赛事/测试链路演练摘要-v1.md` 更新为“战队赛 P0 已闭环”；新增 `炉石赛事/个人赛链路实施清单-v1.md`。对应提交：`dbe7e40`。
- 已推进代码：将炉石赛事实现从 team-only 改为支持 player/team 双分支，涉及 `callback-handler.js`、`notification-utils.js`、`bitable-api-utils.js`；并把本地 `axios` 依赖替换为 Node 内置 `fetch`。对应提交：`7082f3d`。
- 用户明确偏好：在该项目里“遇到问题前不要停”，希望连续推进，不要频繁中断等待确认。该偏好适用于当前炉石赛事系统实现阶段。
- 排查到 OpenClaw Control UI 最近“页面能点但消息不刷新”的问题，高概率是 webchat / Control UI 客户端 WebSocket 重连后订阅失步，而不是 gateway 核心挂死。日志中反复出现 `webchat disconnected code=1001`，随后 `client ready` / `ws client ready` / `chat.history`。
- 用户补充的症状非常关键：卡住时“页面还能点，但消息不刷新”。这支持“前端活着但实时消息流断开”的判断。后续排查优先按 Control UI/WebChat 重连问题处理。
- 用户新增稳定偏好：希望把“防上下文爆炸”作为长期工作习惯。后续在主会话连续施工较久、工具调用很多、读入长内容较多、或已经出现变钝/卡过一次时，主动提醒用户先落摘要/文档/commit，并考虑 `/new` 切换到新会话继续。
- 2026-03-28 夜：个人赛赛果确认链路已继续推进，新增对手确认卡片、超时升级管理员、自动 disputes 衔接、真实表字段校正，以及 smoke 脚本 `scripts/hs-solo-result-smoke.js`。提交：`dbacb54`。
- 2026-03-28 夜：发现当前个人赛测试样本 `solo_test_001`（小甲）与 `solo_test_002`（小乙）绑定的是同一个 `feishu_open_id`：`ou_914e6141a81eb6da2602875aee631269`。这会阻塞真实的“对手确认/拒绝”双人联调，但不影响 submit / timeout / admin-confirm 的单人联调。
