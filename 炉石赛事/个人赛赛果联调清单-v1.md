# 个人赛赛果联调清单 v1

> 日期：2026-03-28

## 1. 当前测试样本

- 赛事：`HS202603-TEST-SOLO`
- 对局：`MATCH-HS202603-TEST-SOLO-001`
- 对阵：小甲 vs 小乙
- 当前目标：验证赛果链路
  - submit
  - opponent confirm / reject
  - timeout escalate
  - admin confirm

---

## 2. 脚本

已新增：`scripts/hs-solo-result-smoke.js`

要求：
- shell 已注入 `FEISHU_ACCESS_TOKEN`
- 当前代码与飞书真实表结构保持同步

---

## 3. 典型命令

### 3.1 提交赛果

```bash
node scripts/hs-solo-result-smoke.js submit \
  --match MATCH-HS202603-TEST-SOLO-001 \
  --operator <side_a_or_side_b_open_id> \
  --result "小甲 3:1 小乙"
```

预期：
- `result_reports` 新建 / 更新 1 条记录
- `report_status = awaiting_opponent_confirmation`
- 对手收到确认卡片

### 3.2 对手确认

```bash
node scripts/hs-solo-result-smoke.js confirm \
  --match MATCH-HS202603-TEST-SOLO-001 \
  --report <result_report_uid> \
  --operator <opponent_open_id>
```

若测试样本双方共用同一个 open_id，可临时使用受控联调模式：

```bash
HS_SOLO_RESULT_TEST_BYPASS=1 \
node scripts/hs-solo-result-smoke.js confirm \
  --match MATCH-HS202603-TEST-SOLO-001 \
  --report <result_report_uid> \
  --operator <same_open_id> \
  --as-side side_b
```

预期：
- `result_reports.report_status = opponent_confirmed`
- `matches.match_status = completed`
- `matches.result_status = confirmed`

### 3.3 对手拒绝

```bash
node scripts/hs-solo-result-smoke.js reject \
  --match MATCH-HS202603-TEST-SOLO-001 \
  --report <result_report_uid> \
  --operator <opponent_open_id> \
  --reason "比分不一致，申请管理员核实"
```

若测试样本双方共用同一个 open_id，可临时使用：

```bash
HS_SOLO_RESULT_TEST_BYPASS=1 \
node scripts/hs-solo-result-smoke.js reject \
  --match MATCH-HS202603-TEST-SOLO-001 \
  --report <result_report_uid> \
  --operator <same_open_id> \
  --as-side side_b \
  --reason "比分不一致，申请管理员核实"
```

预期：
- `result_reports.report_status = opponent_rejected`
- `matches.match_status = disputed`
- `disputes` 自动创建关联记录
- 管理员收到升级卡片

### 3.4 超时升级管理员

```bash
node scripts/hs-solo-result-smoke.js timeout \
  --match MATCH-HS202603-TEST-SOLO-001 \
  --report <result_report_uid> \
  --operator <admin_open_id> \
  --force true \
  --reason "联调时强制模拟超时"
```

预期：
- `result_reports.report_status = awaiting_admin_review`
- `opponent_confirmation_status = timeout`
- `disputes` 自动创建关联记录
- 管理员收到升级卡片

### 3.5 管理员确认

```bash
node scripts/hs-solo-result-smoke.js admin-confirm \
  --match MATCH-HS202603-TEST-SOLO-001 \
  --report <result_report_uid> \
  --admin <admin_open_id>
```

预期：
- `result_reports.report_status = admin_confirmed`
- `matches.match_status = completed`
- `matches.result_status = confirmed`

---

## 4. 本轮已校正的真实表

### result_reports
- 已补关键字段
- 已补状态枚举：
  - `awaiting_admin_review`
  - `timeout`
  - `skipped`

### match_games
- 已按真实字段改写：
  - `game_no`
  - `reported_at`
  - `confirmed_status`
  - `result_reason`

### match_class_states
- 已按真实字段改写为逐职业快照行：
  - `class_state_uid`
  - `side_label`
  - `deck_class`
  - `is_banned / is_used / is_won_locked / is_available`

### disputes
- 已按真实字段改写：
  - `submitted_by_*`
  - `issue_type / issue_summary / issue_detail`
  - `dispute_status`
  - `admin_decision_status`
  - `final_ruling_internal / final_ruling_public`

---

## 5. 2026-03-29 更新：真实双人分支联调结果

### 5.1 第二真实身份样本已补齐

当前样本已完成双人身份拆分：
- 小甲（`solo_test_001`）→ `ou_914e6141a81eb6da2602875aee631269`
- 小乙（`solo_test_002`）→ `ou_83c1ede5ca9e47affd4b337781a6e741`

并已同步补齐：
- `players.feishu_open_id`
- `registrations.submitted_by_open_id`
- `deckSubmissions.submitted_by_open_id`

### 5.2 已跑通的真实双人闭环

#### A. submit -> confirm
- 新赛果：`RPT-MATCH-HS202603-TEST-SOLO-001-1774725587831`
- submit 方：`ou_914e6141a81eb6da2602875aee631269`
- confirm 方：`ou_83c1ede5ca9e47affd4b337781a6e741`

落表结果：
- `result_reports.report_status = opponent_confirmed`
- `result_reports.opponent_confirmation_status = confirmed`
- `result_reports.confirmer_open_id = ou_83c1ede5ca9e47affd4b337781a6e741`
- `matches.match_status = completed`
- `matches.result_status = confirmed`
- `matches.winner_side = side_a`

#### B. submit -> reject
- 新赛果：`RPT-MATCH-HS202603-TEST-SOLO-001-1774725798799`
- 新争议：`DSP-MATCH-HS202603-TEST-SOLO-001-1774725831343`
- reject 方：`ou_83c1ede5ca9e47affd4b337781a6e741`

落表结果：
- `result_reports.report_status = opponent_rejected`
- `result_reports.opponent_confirmation_status = rejected`
- `result_reports.confirmer_open_id = ou_83c1ede5ca9e47affd4b337781a6e741`
- `disputes.submitted_by_side = side_b`
- `disputes.submitted_by_open_id = ou_83c1ede5ca9e47affd4b337781a6e741`
- `disputes.dispute_status = awaiting_admin_decision`
- `matches.match_status = disputed`
- `matches.result_status = disputed`

### 5.3 已跑通的真实管理员收口闭环

#### C. submit -> timeout -> admin-confirm
- 新赛果：`RPT-MATCH-HS202603-TEST-SOLO-001-1774726420380`
- 新争议：`DSP-MATCH-HS202603-TEST-SOLO-001-1774726458350`
- timeout 后由管理员执行 `admin-confirm`

最终落表结果：
- `result_reports.report_status = admin_confirmed`
- `result_reports.opponent_confirmation_status = timeout`
- `matches.match_status = completed`
- `matches.result_status = confirmed`
- `disputes.dispute_status = resolved`
- `disputes.admin_decision_status = approved`

### 5.4 本轮新增执行注意点

如果 `admin-confirm` 需要同时收口 dispute，则喂给统一入口的 `report.json` 必须满足两点：
1. 是 timeout / reject 之后的**最新 report 快照**
2. 其中带有 `linked_dispute_uid`

否则统一入口只会产出 2 条 write_plan（更新 `result_reports` / `matches`），不会额外产出 `update disputes`，导致赛果虽然确认，但 dispute 不会自动收口。

### 5.5 当前结论

这意味着个人赛赛果链路里的三条关键真实样本闭环已经全部补实：
1. `submit -> confirm`
2. `submit -> reject`
3. `submit -> timeout -> admin-confirm`

也就是说，当前不再只是“write_plan 正确”、"模板能生成"或“同一 open_id 受控绕过”，而是：
- **真实双人身份校验**已通过
- **真实争议创建与管理员收口**已通过
- **真实落表结果**已通过

到这一版为止，个人赛赛果链路已经具备“可复跑、可取证、可文档化”的最终收口条件。

---

## 6. 建议下一步

本轮建议已从“继续补链路”切换为“基于收口结果往前推进”：
1. 优先转入个人赛后续主链路或线上接入工作，而不是继续停留在赛果联调本身
2. 若后续还要补管理员改判 / 驳回 / 特殊裁定等细分分支，可直接复用本轮模板与统一入口
3. 如需开新会话继续施工，建议把本轮最终摘要作为新会话的接续上下文，避免主会话继续膨胀
